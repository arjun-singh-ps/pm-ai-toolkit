// A tiny browser-event contract for sibling client components that have no
// direct prop path between them (ChatPanel and RightPanel are both children
// of a server-component layout). No state library needed for one signal.

export const ARTEFACT_RECORDED_EVENT = "pm-toolkit:artefact-recorded";
